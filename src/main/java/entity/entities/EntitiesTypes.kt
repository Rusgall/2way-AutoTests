package entity.entities

enum class abonent_source_type{ DB_LIST, API}
enum class abonent_state{ INVALID_MSISDN, CHECKED, UNCHECKED}
enum class communication_status_type{ DRAFT, ACTIVE, FINISHED, DELETED, STOPPED}
